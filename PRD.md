# Obsidian Spaced Repetition 定制版 PRD

**版本：1.1｜日期：2026-09-17｜状态：需求已确认**

## 1. 背景与目标

基于开源 Obsidian Spaced Repetition 插件，制作供个人使用的桌面定制版，改善以下方面：

- 用明确、精细的时间按钮安排下次复习。
- 自由移动复习窗口，并能同时操作原文。
- 一键定位卡片来源，编辑原文后同步更新卡片。
- 问答卡内的高亮以色块强调，避免作为额外卡片重复复习。

将[上游项目](https://github.com/st3v3nmw/obsidian-spaced-repetition) Fork 到个人 GitHub 账号 `cooperyx`，以开发开始时核实的最新稳定发布版为基础，交付可直接安装到 Obsidian 的插件文件。

## 2. 支持范围

- 仅支持 **Obsidian 桌面端**。
- 基于现有插件定制，保留与本次修改无关的既有功能。
- 兼容已有笔记、卡片及复习数据。

## 3. 功能需求

### 3.1 复习时间按钮

移除 Hard、Good、Easy，保留 Again，并默认完整展示以下按钮：

**Again、1天、2天、3天、5天、7天、10天、14天、30天、60天、90天。**

#### 时间选择规则

- 时间档位支持配置，以上列表作为默认配置。
- 默认全部11个按钮均可见、可操作，无需展开菜单。
- 点击“N天”后，将当前卡片安排在**本次实际复习日期之后的第N个自然日**到期。
- 按本地日期计算，不要求等待完整的 N×24 小时。例如9月15日点击“3天”，到期日期为9月18日。
- 所选间隔直接决定下次到期日期，不再通过 Hard、Good、Easy 评分推算其他日期。
- 保存计划后，继续复习流程。

#### Again 行为

Again 沿用所基于上游版本的行为，包括当前调度状态对应的短期再次复习安排。例如显示1分钟时，按原逻辑安排再次复习，不统一改成固定1分钟。

#### 到期与数据规则

- 卡片到期后进入正常复习的待复习范围，到期不代表自动弹出窗口。
- 错过到期日期的卡片继续保持待复习状态。
- 每张卡片独立保存计划，不覆盖同一文档中其他卡片的安排。
- 关闭、重新打开 Obsidian 后，已保存的计划仍然有效。

### 3.2 可拖动且允许操作原文的复习窗口

- 用户可以按住复习窗口顶部拖动整个窗口。
- 移动过程中保留当前卡片、答案显示状态和复习进度。
- 窗口保持打开时，原文仍可正常获取焦点，支持点击、滚动、选择文字和编辑。
- 用户操作原文后，可以回到当前卡片继续复习。
- 拖动后，窗口内的主要操作按钮仍可访问。

### 3.3 一键打开原文并定位

在复习窗口顶部新增“打开原文”按钮。

点击后：

1. 打开当前卡片所属的 Markdown 文档。
2. 自动滚动到当前卡片对应的内容。
3. 将编辑光标定位到该内容的位置。
4. 保留复习窗口及当前复习状态，允许直接操作原文。

#### 定位与状态规则

- 同一文档包含多张卡片时，准确定位当前正在复习的卡片。
- 打开原文本身不提交复习结果、不切换卡片、不修改到期日期。
- 如果原文件或对应内容已不存在，应明确提示，避免无提示地定位到错误内容。

### 3.4 原文编辑后自动更新卡片

- 用户修改当前卡片对应的 Markdown 内容，待 Obsidian 保存后，复习窗口自动显示更新后的内容。
- 无需关闭重开复习窗口，也无需手动刷新。
- 同步更新问题、答案及其呈现内容。
- 更新时保留当前卡片、答案显示状态和复习进度，不提交复习结果、不改变到期日期。
- 后续复习操作应作用于更新后的卡片，不得将旧内容写回原文。
- 如果编辑导致当前卡片被删除或不再符合卡片语法，应明确提示，并停止对失效旧卡片的评分操作。

### 3.5 问答卡内的高亮仅用于强调

- 对“问题 → 独占一行的 `?` → 答案”格式的多行问答卡，问题和答案内的 `==高亮==` 都属于这一张卡片，不额外生成高亮或挖空复习卡。
- 复习时以彩色背景块显示高亮文字，不显示标记用的双等号；问题面和答案面都支持，编辑保存后同步更新。
- 保留原文中的 Markdown 高亮语法与该问答卡的既有计划，显示样式不回写笔记。
- 问答卡以外的独立高亮继续沿用原来的挖空复习规则。

## 4. 主要操作流程

### 正常复习

打开卡组 → 查看卡片 → 显示答案 → 选择 Again 或时间间隔 → 保存计划 → 继续复习。

### 查阅并修改原文

复习当前卡片 → 点击“打开原文” → 定位对应内容 → 编辑原文 → Obsidian 保存 → 卡片自动更新 → 回到当前卡片继续复习。

### 调整窗口位置

按住复习窗口顶部 → 拖动到需要的位置 → 操作原文或继续复习。

## 5. 交付内容

- 个人 GitHub 账号下的 Fork 仓库及定制源码。
- 明确记录采用的上游版本和定制版本。
- 可直接安装到 Obsidian 的插件文件，包括 `main.js`、`manifest.json`、`styles.css` 等必要文件。
- 安装、启用、更新及恢复上游版本的说明。
- 与本次需求对应的验证结果。

## 6. 验收标准

| 项目         | 通过标准                                                                   |
| ------------ | -------------------------------------------------------------------------- |
| 安装运行     | 定制插件可在 Obsidian 桌面端安装、启用并正常进入复习                       |
| 时间按钮     | 默认完整展示 Again 和10个指定时间档位，均可正常操作                        |
| 配置档位     | 修改时间档位配置后，复习界面按配置展示                                     |
| 自然日调度   | 点击“N天”后，实际保存的到期日期为本次复习日期之后的第N个自然日             |
| Again        | 与所基于上游版本的行为一致                                                 |
| 计划持久化   | 重启 Obsidian 后，卡片到期日期仍然正确                                     |
| 窗口拖动     | 可通过顶部移动窗口，当前卡片和复习状态不丢失                               |
| 原文交互     | 复习窗口打开时，原文仍可获取焦点、滚动和编辑                               |
| 原文定位     | 打开正确文档，滚动并将光标定位到当前卡片对应内容                           |
| 自动同步     | 原文保存后，卡片自动显示修改结果，无需重开或手动刷新                       |
| 状态保持     | 查阅、编辑及同步过程不提交评分、不改变到期日期、不切换当前卡片             |
| 卡片失效     | 删除卡片或破坏卡片语法后有明确提示，不能继续对旧卡片评分                   |
| 数据兼容     | 已有笔记正文、其他卡片及复习记录不因定制功能被错误修改                     |
| 问答卡内高亮 | 卡内高亮不增加卡片数量，复习时显示色块且不露出双等号，卡外独立高亮仍可复习 |

## 7. 本版未纳入的补充功能

移动端适配、窗口大小调整、窗口位置记忆、原文临时高亮不属于本版交付要求。
