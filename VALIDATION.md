# 定制版验证记录

日期：2026-09-15。版本：`1.15.4-cooperyx.1`，基于上游 `1.15.4` / `22fbea0a71ebd39fe29e0bc2d571aea4c42e10e4`。

## 自动验证

环境：家庭 Linux 服务器，Node.js `22.23.1`，pnpm `11.6.0`，按上游 `pnpm-lock.yaml` 冻结安装。使用独立工作目录，不使用 GPU，不部署 HTTP 服务。

| 检查                                 | 结果                                                                      |
| ------------------------------------ | ------------------------------------------------------------------------- |
| TypeScript `--noEmit --skipLibCheck` | 通过                                                                      |
| 完整 Jest 单元测试                   | **48 个套件，412 项全部通过**                                             |
| 有效覆盖率                           | 语句/行 **95.89%**，分支 **93.55%**，函数 **94.80%**                      |
| 上游覆盖率门槛                       | 语句 ≥92%、分支 ≥88%，通过                                                |
| Prettier 全项目检查                  | 通过                                                                      |
| ESLint 全项目检查                    | 0 错误、54 个警告，命令成功；警告主要涉及上游异步和类型约束，未声称无警告 |
| 生产构建                             | 通过，生成插件 JS 与 CSS                                                  |
| `git diff --check`                   | 通过                                                                      |

覆盖率沿用上游排除范围，**不包含完整 Obsidian UI**，不能代替真实桌面验收。

新增测试覆盖以下行为：

- 默认 11 个按钮与每个日期按钮的回调，Again 枚举，按钮禁用、配置更新、Cram 接线。
- OSR/FSRS 手动自然日排期，FSRS 从短期学习转入 Review 状态，其他卡片计划保持。
- 本地实际日期与上游自定义“复习日”不同的场景，以实际日期为准。
- 手动排期和 Again 所用的常规写入路径失败时，恢复原计划并保留队列。
- 问题/答案变更、首尾增删字、上方插入行、重复内容、唯一 block ID 的源位置跟踪。
- 完全相同卡片整块增删存在身份歧义时停止，不误认邻卡；重复卡内部正常编辑仍可同步。
- 同步保持 Card 对象及同胞计划；卡片删除、语法失效后标记为不可评分。
- 精确源行替换、CRLF、字面 `$` 保留，以及原子写入阶段再次编辑时拒绝覆盖。
- 窗口位置约束函数保证可见区域内的正常尺寸窗口不被拖出视口。

## 真实 Obsidian 验收：待完成

本机已安装 Obsidian `1.12.7`。只读检查确认 CLI 当前未启用。另开同应用实例会抢占现有 CLI socket，因此本轮没有启动该实例，也没有修改正式笔记库、全局 CLI 设置或安装目录。

以下项目尚未标记通过：

- 插件在真实桌面加载和启用。
- 实际拖动、原文焦点/滚动/输入、光标定位与异步 Markdown 渲染。
- 保存编辑后浮窗的完整可视更新及状态保持。
- 真正重启 Obsidian 后的计划持久化。

可用独立样例库 [tests/vaults/custom-desktop-smoke](tests/vaults/custom-desktop-smoke/README.md) 按清单补测。也可在 Obsidian 设置 → 通用 → 高级中启用命令行界面后，继续进行自动桌面验收。

## 交付与日志

安装文件位于 `release/obsidian-spaced-repetition/`，安装和恢复说明见 [CUSTOMIZATION.md](CUSTOMIZATION.md)。

完整构建/测试日志保存在本任务的服务器目录，包括 `jest-coverage.log`、`eslint.log`、`prettier.log`、`format.log`。这些日志不包含正式知识库内容。
