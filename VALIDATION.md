# 定制版验证记录

## 当前版本：1.15.4-cooperyx.3

日期：2026-09-17。基于上游 `1.15.4`，在 `.2` 上补充问答卡高亮显示和回归验证。

### 自动验证

家庭服务器本轮两次连接超时，改在 Mac 的独立 scratch 工作区完成冻结依赖安装、测试与构建。环境为 Node.js `22.22.2`、pnpm `11.6.0`，未向系统或项目源码目录安装依赖。

| 检查                                 | 结果                                                 |
| ------------------------------------ | ---------------------------------------------------- |
| TypeScript `--noEmit --skipLibCheck` | 通过                                                 |
| 完整 Jest 单元测试                   | **48 个套件，418 项全部通过**                        |
| 有效覆盖率                           | 语句/行 **95.88%**，分支 **93.56%**，函数 **94.80%** |
| 上游覆盖率门槛                       | 通过；沿用上游排除范围，不包含完整 Obsidian UI       |
| Prettier 全项目检查                  | 通过                                                 |
| ESLint 全项目检查                    | 0 错误、54 个警告，与上版相同                        |
| 生产构建                             | 通过                                                 |

原解析器已经将紧邻的“问题 / ? / 答案”及其中高亮识别为一张问答卡。本轮保留该逻辑，通过新增回归测试固定题干高亮、答案高亮、多处高亮、CRLF、既有计划、正式笔记加载、编辑同步及评分写回行为；显示层增强原生 `<mark>` 色块样式，未用字符串替换处理 Markdown。

### 真实 Obsidian 桌面验收

使用 Mac 上的独立合成测试库，通过 Obsidian CLI、CDP 鼠标/键盘输入、DOM 断言和截图核验。

| 项目     | 实测结果                                                                                    |
| -------- | ------------------------------------------------------------------------------------------- |
| 卡片数量 | 含四处高亮的问答、卡外独立高亮、普通问答共 3 张；类型及子卡数量为 Basic×1、Cloze×1、Basic×1 |
| 问题面   | 2 处高亮均显示背景色块，无语法双等号                                                        |
| 答案面   | 题干及答案共 4 个色块，加粗高亮保留粗体                                                     |
| 高亮样式 | 当前主题背景为 `rgba(255, 208, 0, 0.4)`，内边距与圆角生效                                   |
| 原文编辑 | 实际编辑题干及答案中的高亮文字，保存后自动同步；保持同一个 Card 对象、答案面和卡片数量      |
| 评分写回 | 问答选择 3 天，保存 `2026-09-20,3,250`；原文 `==` 保留，卡外内容不变                        |
| 卡外高亮 | 仍先挖空、再显示答案；不带问答色块样式；另选 7 天保存 `2026-09-24,7,250`，问答计划不变      |
| 普通问答 | 正常显示，无额外高亮元素                                                                    |
| 插件重载 | 重新加载后仍为 3 张卡，两个已保存计划保持，笔记逐字节不变                                   |
| 应用错误 | 本轮 `dev:errors` 未捕获错误                                                                |

新样例见 [HighlightReview.md](tests/vaults/custom-desktop-smoke/HighlightReview.md)。更早版本的拖动、全部日期档位、FSRS 等完整验收见下方历史记录；本轮没有逐项重跑这些未改动功能。

### 正式知识库更新

已自动更新 `obsidian_sync` 中的插件至 `1.15.4-cooperyx.3` 并重新启用。运行时初始化成功，9 个插件命令已注册，三个安装文件的 SHA-256 与实机测试及交付包一致。

原有 71 项设置逐项保留，运行时设置保持一致，插件启用列表未变。启动时插件按既有逻辑将日期缓存 `buryDate` 更新为当天，并把之前仅存在于运行时的默认 `manualReviewDays` 写入设置；其余保存的数据未变。本轮没有在正式库创建测试笔记或执行评分。

更新前的 `.2` 插件、设置和启用列表已备份到本任务 scratch 的 `root/highlight-20260917/retained/20260917-191758/`，其中 `README.json` 记录来源、用途及原文件校验值，应保留供恢复使用。

### 日志与产物

本轮安装包为 `release/obsidian-spaced-repetition-1.15.4-cooperyx.3.zip`，三个安装文件位于 `release/obsidian-spaced-repetition/`，校验值见 `release/SHA256SUMS`。

本轮测试日志、桌面断言及截图保存在统一 scratch 下的 `root/highlight-20260917/`，不纳入安装包。源码、安装包均不依赖 scratch。

## 历史版本：1.15.4-cooperyx.2

日期：2026-09-15。版本：`1.15.4-cooperyx.2`，基于上游 `1.15.4` / `22fbea0a71ebd39fe29e0bc2d571aea4c42e10e4`。

### 自动验证

环境：家庭 Linux 服务器，Node.js `22.23.1`，pnpm `11.6.0`，按上游 `pnpm-lock.yaml` 冻结安装。使用独立工作目录，不使用 GPU，不部署 HTTP 服务。

| 检查                                 | 结果                                                                      |
| ------------------------------------ | ------------------------------------------------------------------------- |
| TypeScript `--noEmit --skipLibCheck` | 通过                                                                      |
| 完整 Jest 单元测试                   | **48 个套件，412 项全部通过**                                             |
| 有效覆盖率                           | 语句/行 **95.88%**，分支 **93.55%**，函数 **94.80%**                      |
| 上游覆盖率门槛                       | 语句 ≥92%、分支 ≥88%，通过                                                |
| Prettier 全项目检查                  | 通过                                                                      |
| ESLint 全项目检查                    | 0 错误、54 个警告，命令成功；警告主要涉及上游异步和类型约束，未声称无警告 |
| 生产构建                             | 通过，生成插件 JS 与 CSS                                                  |
| `git diff --check`                   | 通过                                                                      |

覆盖率沿用上游排除范围，**不包含完整 Obsidian UI**，不能代替真实桌面验收。

新增测试覆盖以下行为：

- 默认 11 个按钮与每个日期按钮的回调，Again 枚举，按钮禁用、配置更新、Cram 接线。
- OSR/FSRS 手动自然日排期，FSRS 从短期学习转入 Review 状态，其他卡片计划保持。
- 本地实际日期与上游自定义“复习日”不同的场景，以实际日期为准。
- 手动排期和 Again 所用的常规写入路径失败时，恢复原计划并保留队列。
- 问题/答案变更、首尾增删字、上方插入行、重复内容、唯一 block ID 的源位置跟踪。
- 完全相同卡片整块增删存在身份歧义时停止，不误认邻卡；重复卡内部正常编辑仍可同步。
- 同步保持 Card 对象及同胞计划；卡片删除、语法失效后标记为不可评分。
- 精确源行替换、CRLF、字面 `$` 保留，以及原子写入阶段再次编辑时拒绝覆盖。
- 窗口位置约束函数保证可见区域内的正常尺寸窗口不被拖出视口。
- 正式笔记加载路径保留源快照，避免未编辑卡片首次同步时失效。

### 真实 Obsidian 验收：通过

环境：macOS，Obsidian `1.13.7`（安装器 `1.12.7`），本地时区 `Asia/Shanghai`。通过 Obsidian CLI、CDP 鼠标/键盘事件和真实应用截图验证。使用独立的合成测试库，没有安装到正式知识库或修改正式笔记。

| 项目          | 实测结果                                                                         |
| ------------- | -------------------------------------------------------------------------------- |
| 安装与加载    | `1.15.4-cooperyx.2` 正常加载并进入复习，新会话卡片有效                           |
| 默认日期按钮  | Again 与 1/2/3/5/7/10/14/30/60/90 天全部可见、可点击，自动换行                   |
| 配置日期档位  | 在设置输入框改成 `1, 4, 12`，复习按钮随之更新；测试后恢复默认                    |
| 自然日调度    | 9 月 15 日点击 3 天，实际保存 `2026-09-18,3,250`，随后进入下一张                 |
| OSR Again     | 实际点击后保存 `2026-09-15,0,230`，与当前上游调度计算一致                        |
| FSRS Again    | 实际到期时间减去本次复习时间为 60 秒，与按钮上的 1 分钟一致                      |
| FSRS 手动日期 | 点击 10 天，保存本地 9 月 25 日零点，间隔为 10、状态为 Review                    |
| 计划持久化    | 应用进程关闭后重新打开，测试笔记逐字节相同，已保存日期保持                       |
| 浮窗拖动      | 真实鼠标拖动保持答案和进度；四个边缘均受约束，评分按钮可访问                     |
| 原文交互      | 正确进入源代码编辑模式；光标位于当前卡首行；窗口外滚轮使编辑器滚动 400px         |
| 输入与焦点    | 输入 `0`、`J`、`S`、空格正常保存，不触发复习；同步后编辑器保留焦点               |
| 编辑同步      | 修改问题、答案和 Markdown 粗体内容自动更新；卡面/答案状态、队列与原计划保持      |
| 源位置变化    | 原文上方插入两行后，光标定位从第 22 行准确更新至第 24 行                         |
| 重复卡片      | 编辑第二张同名卡并评分，只更新第二张；第一张正文和计划不变                       |
| 正反向同胞卡  | 正向卡设为 5 天后，反向卡仍未评分、没有继承正向计划                              |
| 卡片语法失效  | 删除分隔符后明确提示，所有评分按钮禁用；快捷键跳过仍可继续                       |
| 原文件删除    | 删除当前测试文件后明确提示，所有评分按钮禁用                                     |
| 会话清理      | 连续打开复习保持一个浮窗；编辑期间停用插件后窗口关闭、原文监听清空，继续输入正常 |
| 运行错误      | 上述最终测试阶段 Obsidian `dev:errors` 未捕获错误                                |

#### 本次实机发现并修复的问题

1. 正式队列使用的 `NoteFileLoader` 未保存原文快照，导致未编辑卡片在首次同步时被误判为失效。补齐快照，并加入正式加载路径的回归断言；实机重新安装构建后通过。
2. 新版 Obsidian 的原生关闭按钮与插件工具栏关闭按钮同时出现。补充样式，保留工具栏关闭入口。

调度写入仍沿用上游对当前卡片文本的格式整理，例如去除该卡片末尾空格；实测其他卡片正文不变。完整重复块增删、自定义复习日与实际日期不同等组合由单元测试覆盖，本轮没有逐一重复桌面验收。旧版 Obsidian 和其他操作系统未在本轮实测。

复测清单见 [tests/vaults/custom-desktop-smoke](tests/vaults/custom-desktop-smoke/README.md)。

### 交付与日志

安装文件位于 `release/obsidian-spaced-repetition/`，安装和恢复说明见 [CUSTOMIZATION.md](CUSTOMIZATION.md)。

完整构建/测试日志保存在本任务的服务器目录，本轮包括 `desktop-jest.log`、`desktop-eslint.log`、`desktop-prettier.log`。桌面测试的 CLI 日志、断言记录和截图保存在当前任务的外部 scratch 目录，不纳入插件包。这些日志不包含正式知识库内容。
